def welcome_user (user_name,user_age):#we use this to start defining the function
    print("Hello " + user_name) #ensure indentation is present
    print("You are " + str(user_age) + " years old. Welcome")
welcome_user("Kabugi", 22)#retype the name of the function and remember ()